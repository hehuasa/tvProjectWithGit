import React, { PureComponent } from 'react';

export default class List extends PureComponent {
  state = {

  };
  componentDidMount() {
  }
  render () {
    return (
     <div>我是header</div>
    )
  }
}